specificationVersion: jobtemplate-2023-09
name: TestJob
steps:
- name: Step1
  stepEnvironments:
  - name: MyEnv
    script:
      actions:
        onEnter:
          command: "{{Task.File.script}}"
  script:
    embeddedFiles:
    - name: script
      type: TEXT
      data: "echo hello"
    actions:
      onRun:
        command: "{{Task.File.script}}"
