specificationVersion: jobtemplate-2023-09
name: TestJob
parameterDefinitions:
- name: invalid-name
  type: STRING
steps:
- name: Step1
  script:
    actions:
      onRun:
        command: python
        args:
        - "-c"
        - "print('{{Param.invalid-name}}')"
