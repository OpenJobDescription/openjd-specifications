specificationVersion: jobtemplate-2023-09
name: TestJob
steps:
- name: Step1
  stepEnvironments:
  - name: MyEnv
    script:
      embeddedFiles:
      - name: setup
        type: TEXT
        data: "setup script"
      actions:
        onEnter:
          command: "{{Env.File.setup}}"
  script:
    actions:
      onRun:
        command: "{{Env.File.setup}}"
