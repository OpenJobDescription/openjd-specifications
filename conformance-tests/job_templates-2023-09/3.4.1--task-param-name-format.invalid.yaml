specificationVersion: jobtemplate-2023-09
name: TestJob
steps:
- name: Step1
  parameterSpace:
    taskParameterDefinitions:
    - name: invalid-name
      type: STRING
      range:
      - value1
  script:
    actions:
      onRun:
        command: echo
        args:
        - "{{Task.Param.invalid-name}}"
