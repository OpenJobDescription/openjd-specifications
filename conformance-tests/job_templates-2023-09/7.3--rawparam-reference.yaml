specificationVersion: jobtemplate-2023-09
name: TestJob
parameterDefinitions:
- name: MyPath
  type: PATH
  default: /some/path
steps:
- name: Step1
  script:
    actions:
      onRun:
        command: echo
        args:
        - "{{RawParam.MyPath}}"
