specificationVersion: jobtemplate-2023-09
name: "Job-{{Param.Version}}-{{Param.Project}}"
parameterDefinitions:
- name: Version
  type: STRING
- name: Project
  type: STRING
steps:
- name: Step1
  script:
    actions:
      onRun:
        command: echo
        args:
        - "JOBNAME:Job-{{Param.Version}}-{{Param.Project}}"
