specificationVersion: jobtemplate-2023-09
extensions:
  - FEATURE_BUNDLE_1
name: TestJob
parameterDefinitions:
- name: CpuMin
  type: INT
  minValue: 1
- name: CpuMax
  type: INT
  minValue: 1
steps:
- name: Step1
  hostRequirements:
    amounts:
    - name: amount.worker.vcpu
      min: "{{Param.CpuMin}}"
      max: "{{Param.CpuMax}}"
  script:
    actions:
      onRun:
        command: python
        args:
        - "-c"
        - "print()"
