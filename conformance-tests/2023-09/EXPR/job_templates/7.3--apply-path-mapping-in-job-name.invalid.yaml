# apply_path_mapping() is only available in host context, not in job name (submission-time)
specificationVersion: jobtemplate-2023-09
extensions:
- EXPR
name: "{{ apply_path_mapping('/mnt/share') }}"
steps:
- name: Step1
  script:
    actions:
      onRun:
        command: python
        args:
        - "-c"
        - "print()"
