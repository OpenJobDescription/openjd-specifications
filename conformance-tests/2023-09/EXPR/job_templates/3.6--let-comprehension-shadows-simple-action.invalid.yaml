specificationVersion: jobtemplate-2023-09
extensions:
- EXPR
- FEATURE_BUNDLE_1
name: TestJob
parameterDefinitions:
- name: Items
  type: LIST[INT]
  default: [1, 2, 3]
steps:
- name: Step1
  bash:
    let:
    - x = 10
    script: "echo {{ [x for x in Param.Items] }}"
