template:
  specificationVersion: jobtemplate-2023-09
  name: TestJob
  steps:
  - name: Step1
    script:
      actions:
        onRun:
          command: python
          args:
          - -c
          - print(r'SESSION_DIR:{{Session.WorkingDirectory}}')
expected:
  output_posix:
  - SESSION_DIR:/
  output_windows:
  - "SESSION_DIR:"
  - ":\\"
