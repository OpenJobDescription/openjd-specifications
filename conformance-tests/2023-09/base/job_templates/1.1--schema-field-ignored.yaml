specificationVersion: jobtemplate-2023-09
$schema: "https://example.com/openjd-schema.json"
name: TestJob
steps:
- name: Step1
  script:
    actions:
      onRun:
        command: python
        args:
        - "-c"
        - "print()"
