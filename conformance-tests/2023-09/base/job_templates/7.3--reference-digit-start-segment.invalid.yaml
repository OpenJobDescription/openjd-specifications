# A reference segment starting with a digit ({{Param.1X}}) is not a valid Identifier and must be rejected.
specificationVersion: jobtemplate-2023-09
name: TestJob
steps:
- name: Step1
  script:
    actions:
      onRun:
        command: echo
        args:
        - "{{Param.1X}}"
