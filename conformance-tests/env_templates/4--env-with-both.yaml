specificationVersion: environment-2023-09
environment:
  name: TestEnv
  variables:
    MY_VAR: value
  script:
    actions:
      onEnter:
        command: python
        args:
        - "-c"
        - "print()"
      onExit:
        command: python
        args:
        - "-c"
        - "print()"
