specificationVersion: jobtemplate-2023-09
name: TestJob
parameterDefinitions:
- name: Config
  type: STRING
  default: job-default
steps:
- name: Step1
  script:
    actions:
      onRun:
        command: python
        args:
        - "-c"
        - "print(r'MERGE:Config={{Param.Config}}')"
